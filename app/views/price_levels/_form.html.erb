<p>
  <strong>Brand:</strong>
  <%= @brand.name %>
</p>

<%= form_for [@brand, @price_level] do |f| %>
  <% if @price_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@price_level.errors.count, "error") %> prohibited this order type from being saved:</h2>

      <ul>
      <% @price_level.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="input-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: 'form-control' %>
  </div>
  <div class="actions">
    <%= f.submit class: 'btn btn-primary' %>
  </div>
<% end %>
