<p id="notice"><%= notice %></p>

<p>
  <strong>Brand:</strong>
  <%= @brand.name %>
</p>

<p>
  <strong>Name:</strong>
  <%= @price_level.name %>
</p>

<%= link_to 'Edit', edit_brand_price_level_path(@brand, @price_level) %> |
<%= link_to 'Back', brand_path(@brand) %>
